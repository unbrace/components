import * as React from 'react';
import { storiesOf } from '@storybook/react';

storiesOf('Welcome', module).add('to the storybook of Unbrace !', () => (
  <h1>Welcome to the storybook of Unbrace</h1>
));
